# JISP – Jacobs Spatial Intelligence Platform
# Runtime dependencies will be populated as implementation steps proceed.
# Approved stack only (see README.md): Python, PostgreSQL, PostGIS, TimescaleDB,
# GDAL/OGR, GeoPandas, Rasterio, xarray, scikit-learn, PySAL, HDBSCAN/DBSCAN,
# OpenGeoAI, OpenCL (PyOpenCL/Numba), SHAP, Ollama, Llama 3.3, FastAPI, Pydantic,
# MapLibre GL JS (frontend), Streamlit.

# --- Step 2: minimal FastAPI app ---
fastapi>=0.110
pydantic>=2.6
uvicorn[standard]>=0.29  # ASGI server required to run FastAPI

# --- Step 5: Database & Audit Logging ---
sqlalchemy>=2.0  # ORM for PostgreSQL audit persistence
psycopg2-binary>=2.9  # PostgreSQL adapter
